import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  roundedImage: {
    width: 50,
    height: 50,
    borderRadius: 25,
    overflow: 'hidden',
    borderWidth: 1,
  },
})
